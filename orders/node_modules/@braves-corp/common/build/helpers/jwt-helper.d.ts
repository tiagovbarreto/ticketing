export declare class JWTHelper {
    static generateAuthToken(user: any): Promise<string>;
    static verify(token: string): string | object | null;
    static toHash(password: string): Promise<string>;
    static compare(storedPassword: string, suppliedPassword: string): Promise<boolean>;
}
